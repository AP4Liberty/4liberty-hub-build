# Secrets — never commit these
deploy/.env*
*.pem
*.key

# OS/editor junk
.DS_Store
Thumbs.db
*.log

# Local WP dev environment, if one gets added later
/wordpress/
/.wp-env.json
node_modules/

# Netlify local state — never user code
.netlify/

# Build artifacts — regenerate with `python deploy/make_zips.py`
deploy/dist/
